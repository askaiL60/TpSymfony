{% extends 'base.html.twig' %}

{% block title %}Modifier les rôles de {{ user.email }}{% endblock %}

{% block body %}
<h1>Modifier les rôles de {{ user.email }}</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">Mettre à jour</button>
{{ form_end(form) }}

<a href="{{ path('admin_users') }}">← Retour à la liste</a>
{% endblock %}
